---
name: kin-ruby
description: Used to help make sure ruby code is using Kin conventions and patterns.
---

# Syntax Guidance

- Methods which mutate state should have trailing `!`
- Query methods should be named with a trailing `?`
- Instance variables should be private from external read and writes
- Use single quotes when string is not interpolated
- Use double quotes when string is interpolated
