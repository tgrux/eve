## 2026-02-17 — Full application scaffold: backend API + frontend UI

### What was built
- **Backend** (`src/server/`):
  - `db.js`: JSON file-based persistence (better-sqlite3 unavailable; no Python in sandbox)
  - `tasks.js`: CRUD logic — createTask, getAllTasks, updateTask, deleteTask with validation
  - `index.js`: Express server with all 4 REST endpoints (GET/POST/PUT/DELETE /api/tasks)

- **Frontend** (`src/client/`):
  - `App.svelte`: Full single-page app with create, toggle complete, inline edit, delete
  - Due date display with past-due red flagging
  - Priority badge (Low/Medium/High) with color coding
  - `src/lib/api.js`: fetch-based API client

- **Config**:
  - `package.json`, `vite.config.js`, `tsconfig.json`, `tailwind.config.js`, `postcss.config.js`
  - `index.html` (Vite entry point)

### Tests
- 18 unit tests covering createTask, getAllTasks, updateTask, deleteTask — all pass
- TypeScript check (tsc --noEmit) — 0 errors on server code

### Features completed
All v1 features implemented: Create, Complete, Edit, Delete tasks; Due dates with overdue flagging; Priority levels with badges.
