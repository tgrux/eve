# pin.md — System Context

This file describes the resources and constraints of the environment we are building in. 

## Environment

- **Sandbox** — no network calls, no pushes to GitHub
- **Local only** — everything runs on your machine
- **Runtime** — Colima (Docker container)
