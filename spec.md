Setup a command in the CLI that allows me to see what is available to the AI agent I'm using.  This might be claude or codex.   I want to list out the MCPs, Hooks, Skills, if in Plan mode, and (if using claude) slash commands.

Setup a command to detect what I have in my ~/.claude or ~/.codex folder as far as skills, hooks, mcps, and if claude, slash commands.  Basically anything I have setup globally.
